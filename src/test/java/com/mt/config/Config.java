package com.mt.config;

import org.openqa.selenium.WebDriver;

public class Config
{
	//showing error becz dependences or jars of selenium standalone is not add to proj
	
public static WebDriver driver;
}
